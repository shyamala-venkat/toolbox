/**
 * Stub for a future custom title bar.
 *
 * Phase 2 uses Tauri's native window chrome — rendering this component is a
 * no-op. The export exists so Phase 4 can swap in a real implementation
 * without callers changing their imports.
 */
export function TitleBar() {
  return null;
}
